import express from "express"; import cors from "cors"; import authRoutes from "./routes/authRoutes"; import userRoutes from "./routes/userRoutes"; import zoneRoutes from "./routes/zoneRoutes"; import scheduleRoutes from "./routes/scheduleRoutes"; import residentRoutes from "./routes/residentRoutes"; import { errorHandler } from "./middleware/errorHandler";
const app=express(); app.use(cors({origin:process.env.CLIENT_ORIGIN||"http://localhost:3000"})); app.use(express.json({limit:"4mb"})); app.get("/health",(_req,res)=>res.json({status:"ok"})); app.use("/api/auth",authRoutes); app.use("/api/users",userRoutes); app.use("/api/zones",zoneRoutes); app.use("/api/schedules",scheduleRoutes); app.use("/api/resident",residentRoutes); app.use(errorHandler); export default app;
