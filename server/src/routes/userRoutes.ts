import { Router } from "express"; import { selectZone,updateProfile } from "../controllers/userController"; import { authenticate } from "../middleware/authenticate"; import { authorize } from "../middleware/authorize"; import { validateRequest } from "../middleware/validateRequest"; import { profileSchema,zoneSelectionSchema } from "../validators/authValidator";
const router=Router(); router.patch("/me/zone",authenticate,authorize("resident"),validateRequest(zoneSelectionSchema),selectZone); router.patch("/me/profile",authenticate,authorize("resident"),validateRequest(profileSchema),updateProfile); export default router;
