"use client";
import { api } from "@/lib/api"; import { setToken } from "@/lib/auth-storage"; import { useRouter } from "next/navigation"; import { useState } from "react";
export default function Login() { const router=useRouter(); const [error,setError]=useState(""); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const f=new FormData(e.currentTarget); try { const result=await api.login({email:String(f.get("email")),password:String(f.get("password"))}); setToken(result.token); router.push(result.user.zoneId ? "/dashboard" : "/select-zone"); } catch(err){setError(err instanceof Error?err.message:"Unable to sign in");}} return <form onSubmit={submit}><h1>Sign in</h1>{error&&<p className="error">{error}</p>}<input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" required/><button>Sign in</button></form>; }
